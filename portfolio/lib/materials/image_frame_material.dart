import 'package:flutter/widgets.dart';

class ImageFrameMaterial extends StatelessWidget {
  const ImageFrameMaterial({
    super.key
  });
  
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}